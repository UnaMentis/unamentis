import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Security headers applied to all responses. (A Content-Security-Policy is a
  // worthwhile addition but must be tuned against the running app before
  // enforcing; tracked as a follow-up.)
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'Referrer-Policy',
            value: 'strict-origin-when-cross-origin',
          },
          {
            key: 'Strict-Transport-Security',
            value: 'max-age=31536000; includeSubDomains',
          },
        ],
      },
    ];
  },
};

export default nextConfig;
