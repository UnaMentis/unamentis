# Execution Log: 2026-06-10 overnight session

Companion to `BETA_EXECUTION_PLAN_2026-06-10.md`. Every change made during the overnight execution session is recorded here: what changed, which files, how it was verified, and anything discovered along the way. Nothing in this session is committed; all changes sit in the working trees for human review.

Repos in scope:
- `unamentis` (this repo, branch `feature/pre-beta-updates`)
- `unamentis-ios` (has pre-existing uncommitted user work: barge-in coordinator, STT model manager, session view edits; that work is preserved untouched unless explicitly noted)

---

## Phase 0: Verification pass (read-only)

8 parallel verification agents confirmed/refuted each planned work item against the current working trees before any fix was attempted, because uncommitted in-flight work may have already addressed some findings. Results recorded per item below as they complete.

### Phase 0 results (verification, 8 agents, completed ~23:05)

Headline status changes vs. the plan as originally drafted:
- Already fixed by prior/in-flight work (no action needed): B7 WebSocket auth, rate-limiter cold start, web-client refresh-token cookie (httpOnly migration plus legacy-key purge), iOS LICENSE file, ITSAppUsesNonExemptEncryption, usage-description wording, Core Data file protection, APIKeyManager fallback, the silent barge-in PCM bug (superseded by the in-flight BargeInCoordinator work), KB parallel audio pipelines.
- Partially fixed, completion in flight tonight: B9 (age gate enforced server-side; consent_records writes missing), B10 (intake IPs coarsened; auth-path raw IPs and disclosure remain), CSP (report-only shipping; no reporting endpoint; feature-flag origin missing from connect-src), denial-of-wallet (count-based in-memory caps; no spend ceiling), privacy manifest (DiskSpace declared; SystemBootTime used but undeclared), transcript release logging (partially demoted).
- Confirmed open, being fixed tonight: full telemetry chain server-side (in-memory only, no error metric, no TTFA field, no allow-list, silent mock fallback in console, dead proxy routes) and iOS-side (dead serverIP key, cleartext-only endpoint, TTFA never uploaded, no consent gate, IDFV exporter in release builds).
- Significant discoveries (added to scope tonight): the Operations Console sends no auth token, so a beta-configured server would silently render mock data everywhere; metrics intake has no byte caps (memory amplification); session counts are inflated; WS tokens passed as query params can leak into access logs; iOS has no registration/auth integration at all, so the server-side age gate has no path on TestFlight (tonight's fix: local 13+ attestation plus telemetry consent in onboarding, revocable in Settings).
- Discoveries deferred with reasons (morning decisions): web-client Deepgram/ElevenLabs provider paths expect raw API keys in the browser against endpoints that do not exist (needs a token-minting design, not a night fix); no crash reporting (recommend Apple-native MetricKit collector rather than a third-party SDK, fits the privacy posture); USM Core crash supervision (large Rust change, post-beta per the 2026-05-31 plan, and services.toml has uncommitted user edits); Pocket TTS server download stub.

### Website confidentiality action (~23:15)

Verification discovered that the live site serves git-tracked internal markdown at public URLs (unlinked but crawlable), including an angel-investor strategy document, a pitch document, strategy reviews, and a stale PROJECT_OVERVIEW copy. Action taken in /Users/ramerman/dev/unamentis.org (its GitHub remote is private; exposure is only via Cloudflare Pages serving tracked files):
- Moved 21 markdown files (all internal docs; README.md and CLAUDE.md retained) to the private space at ~/Projects/UnaMentis/website-docs/, preserving the user's uncommitted edits to one of them and one untracked in-progress file.
- Staged the 20 tracked deletions. Deploys are git-driven, so the takedown goes live on the next push. Cloudflare refuses CLI direct-uploads for git-connected Pages projects, so this could not be deployed tonight.
- MORNING ACTION #1: review, commit, push unamentis.org, then purge the Cloudflare cache for /docs/* and the removed root files.

### Execution launched (~23:20)

- Website agent: privacy.html + terms.html from the about.html template, content sourced from the web-client privacy/terms pages and SUBPROCESSORS.md; footer links across all pages.
- Server workflow (4 lanes + verify): consent records + policy acceptance + auth-IP disclosure/retention; CSP reporting endpoints + enforcing header; telemetry persistence (SQLite store), allow-list, byte caps, error/TTFA fields, session dedupe, WS token log redaction, daily spend ceiling; console live-vs-mock banner, auth token support, P99/TTFA/error display, dead route removal. Verify stage runs pytest, lint, and web builds.
- iOS workflow (2 lanes + verify): telemetry key/HTTPS/TTFA/error fields/consent gating/IDFV compile-out/manifest/dependency pin; onboarding 13+ attestation + telemetry consent UI + Settings privacy section + transcript log demotion. Verify stage runs xcodegen, full build, quick tests, lint.
- On-device LLM (after the iOS workflow settles, to avoid project.yml collisions): llama.cpp b7263 xcframework (pre-verified: simulator slice, all 21 required symbols, mistral3+gemma3 archs) into UnaMentis/Frameworks/, LLAMA_AVAILABLE enabled for Debug+Release, un-exclude OnDeviceLLMService.swift, drop the dead 2.0GB wrong-model bundle resource, fix the 512-token batch overflow, copy the verified Ministral-3-3B GGUF into the simulator app container, then prove generation on the Conversation Test screen. Gemma 3 4B GGUF confirmed compatible with this exact path but deferred (needs a chat-template branch); MLX/Qwen3 remains the post-beta direction per docs/ios/ON_DEVICE_LLM_AUDIT_2026-05.md.

### Website privacy/terms pages complete (~23:30)

Created in /Users/ramerman/dev/unamentis.org (unstaged, for review): privacy.html and terms.html (version 1.0, effective 2026-06-10), built from the about.html template with the existing nav/footer/i18n chrome. Footer "Privacy · Terms" links added to all 12 chrome-bearing pages (kiosk.html intentionally skipped, it is a fullscreen QR display). Content follows the web-client policy pages and SUBPROCESSORS.md: aggregate-only telemetry with per-install random ID, IP coarsening, sub-processor disclosure link, privacy tiers (cloud, self-hosted, on-device), 13+ self-attestation, export/deletion via info@unamentis.org, plus a website-specific section disclosing the beta form's Google Apps Script storage (verified in js/beta-form.js). Terms titled "Terms of Use" (the site offers no services), MIT-consistent as-is language, beta caveats, both marked as beta drafts pending legal review. Verified: tag-balance parse on all 15 root HTML files, all links resolve, no em dashes introduced, no commercial terms.

Follow-ups recorded for the docs pass: converge the web-client privacy contact line to info@unamentis.org, align policy dates (web-client pages say 2026-05-31), and converge web-client terms with the website terms after legal review.

### iOS workflow complete (~00:00)

Both lanes plus verification green. Build succeeds; 1,465 unit tests executed with 1,463 passing (the 2 failures are pre-existing, root-caused to committed history: a retuned ResponseIntent classifier and a model-file-dependent OnDeviceLLMModelManager test); SwiftLint 0 violations in 376 files. Telemetry chain: dead serverIP keys fixed in three sites, HTTPS-capable telemetryEndpointURL, TTFA median/p99 and typed per-stage error counters in the payload, consent-gated uploads verified end to end with a traced call path (UI writes the key; SessionManager and MetricsUploadService both enforce it; revocation stops uploads mid-session), MetricsExporter proven absent from Release at the symbol level (0 IDFV references). Consent UI: new onboarding consent page (blocking 13+ attestation, optional telemetry default-off, policy links) plus a revocable Settings Privacy section. PrivacyInfo gained the SystemBootTime declaration; SwiftReadability pinned by revision with Package.resolved made trackable; remaining transcript-content logging demoted to debug in the lane's files. CRITICAL DISCOVERY: scripts/test-quick.sh and test-ci.sh execute ZERO tests (a filter names a nonexistent class) and exit 0, so their pass signal was vacuous; fix assigned to the docs/scripts lane. Skipped with reason: AVAudioSession consolidation (needs the in-flight barge-in refactor to land first).

### Server workflow complete (~00:10)

All four lanes plus verification green on the FULL suite: 3,013 management tests pass, lint clean, both Next apps build with eslint 0 errors and tsc clean. Consent: registration now writes versioned consent_records (age, terms, privacy) with hashed coarsened IP, sets age_verified_at, requires explicit policy acceptance (new checkbox + 48/48 auth component tests); HUMAN ACTION: apply server/database/migrations/005_consent_registration_categories.sql with psql before live registrations work. Privacy: 90-day auth IP retention sweep implemented and verified live against real PostgreSQL (first sweep scrubbed 7 audit rows, 2 token rows); privacy page discloses security-log retention. CSP: enforcing header now ships alongside report-only on BOTH apps with /api/csp-report reporting endpoints (live header checks pass); also discovered and fixed a pre-existing production build blocker (test-only export in the realtime token route had broken next build since it landed). Telemetry: SQLite-backed persistence with hot cache (survives restart), strict field allow-list plus byte caps (64KB body cap, byte-aware cache), error-count/error-rate/TTFA/P99 in intake and aggregates, unique-session dedupe persisted across restarts, WS token redaction in access logs, durable daily realtime spend budget endpoints (default 200 mints/day). Console: live-vs-mock provenance store with visible banner, Authorization header support (localStorage token with env fallback, 401 surfaces "authentication required" instead of silent mock data), 8-card metrics panel with TTFA and error rate, dead proxy routes deleted.

### On-device LLM: WORKING in the simulator (~00:10)

The challenge goal is met. Changes: official llama.cpp b7263 xcframework added at UnaMentis/Frameworks/ (pre-verified simulator slice and full symbol set); LLAMA_AVAILABLE added to Debug AND Release compilation conditions; OnDeviceLLMService.swift un-excluded from the build; the dynamic framework embedded; the dead 2.0GB wrong-model bundle resource removed (cuts every install by 2GB); the 512-token batch-overflow bug fixed with prompt-sized batch allocation plus a context-window guard; stop-sequence handling added so generation ends at chat-template markers. Verified end to end on the iPhone 17 Pro simulator with the verified Ministral-3-3B-Instruct GGUF (APFS-cloned into the app container): Settings shows "On-Device LLM, Ready"; the Conversation Test screen logged "[DEBUG] localMLX selected - using OnDeviceLLMService", "OnDeviceLLMService initialized with model path: .../Documents/models/LLM/Ministral-3-3B-Instruct-2512-Q4_K_M.gguf", and "LLM service type: OnDeviceLLMService"; the app answered "What is photosynthesis?" and "Explain why the sky is blue" with correct streamed answers; process RSS measured at 4.36GB (the 2.1GB model resident in-process); the raw [INST] template bleed observed in the second answer (impossible from an Ollama response) independently confirms raw in-process llama.cpp generation and motivated the stop-sequence fix. Screenshots: /tmp/ondevice-llm-ready.png, /tmp/conv-test.png, /tmp/conv-test3.png. Turn latency ~20-32s (CPU-only simulator; real devices use Metal). Bonus live verifications during the demo: the new onboarding consent gate rendered and blocked correctly (Get Started disabled until attestation), and the consent-gated telemetry timer fired at 300s, correctly queueing metrics.

KNOWN ISSUES recorded for the morning: (1) the onboarding age toggle did not respond to synthetic idb taps while the adjacent telemetry toggle did; verify by hand, a real-finger repro would block onboarding; (2) the Conversation Test header shows the settings model name (qwen2.5:14b-instruct) instead of the actual on-device model (display-only); (3) OnDeviceLLMSettingsView Load Model button remains a stub; (4) each call site constructs a fresh service instance (2GB reload; needs a shared instance); (5) no SHA256 verification on model downloads.

<!-- Log entries appended chronologically below -->
